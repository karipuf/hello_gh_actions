name: Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
         echo "Hello, World!"
         echo "How are you doing?"
         echo "`pwd`"
         echo "chicken=pie" >> $GITHUB_ENV
      - name: Access Env
        run echo $chicken